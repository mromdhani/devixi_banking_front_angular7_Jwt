import { ComptesFilterPipe } from './comptes-filter.pipe';

describe('FilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ComptesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
